-- Reconcilia un estado legacy donde V7 quedo registrada como fallida,
-- pero los objetos ya existen en esquema por ejecuciones previas.
-- Mantiene el versionado consistente sin alterar datos.
SELECT 1;
